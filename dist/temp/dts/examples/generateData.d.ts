import { GraphElements } from '../index';
export interface NodeData {
    label: string;
}
export declare function generateData(nodeNumber?: number, linkDublicationNumber?: number, positionRange?: number): GraphElements;
